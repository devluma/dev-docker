# -----------------------------------------------------------------
# Docker
# -----------------------------------------------------------------

# ignora tudo no diretório "htdocs"
htdocs/*
!htdocs/.gitkeep

# ignora tudo no diretório "storage"
storage/data/*
!storage/data/.gitkeep
storage/logs/*
!storage/logs/.gitkeep

# ignora tudo no diretório "src/bundles"
src/bundles/*
!src/bundles/.gitkeep

# ignora tudo no diretório "src/box-builds/nginx/sites", exceto:
src/box-builds/nginx/sites/*
!src/box-builds/nginx/sites/*.local.conf

# -----------------------------------------------------------------
# Default
# -----------------------------------------------------------------

# ignora todos os arquivos iniciados com .
.*

# rastreia o arquivo .gitignore (Não ignorar isso)
!.gitignore

# rastreia o arquivo .gitkeep (Não ignorar isso)
!.gitkeep

# rastreia o arquivo .editorconfig (Não ignorar isso)
!.editorconfig

# rastreia o arquivo readme.md (Não ignorar isso)
!readme.md

# rastreia o arquivo .gitattributes (Não ignorar isso)
!.gitattributes

# rastreia o arquivo .env-example (Não ignorar isso)
!.env-example

# rastreia o arquivo .htaccess (Não ignorar isso)
!.htaccess

# ignora todos os arquivos que começam com ~
~*

# ignora arquivos gerados pelo sistema operacional
ehthumbs.db
Thumbs.db

# ignora arquivos de Editores
*.sublime-project
*.sublime-workspace
*.komodoproject
.idea

# ignora arquivos de log e banco de dados
*.log
*.sql
*.sqlite

# ignora arquivos compilados
*.com
*.class
*.dll
*.exe
*.o
*.so

# ignora arquivos de pacote
*.7z
*.dmg
*.gz
*.iso
*.jar
*.rar
*.tar
*.zip

# ignora arquivos APPLE
.DS_Store